I built BackupProof: an open-source backup readiness checker for self-hosted Docker Compose stacks.

It scans docker-compose.yml for persistent volumes, bind mounts, database services, missing backup hints, and missing restore-test signals.

Runs locally. Does not run backups.

https://github.com/kaibuilds/backupproof
